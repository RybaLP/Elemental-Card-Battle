import { GameSession } from "@/types/gameSession"
import { useGameSessionStore } from "@/store/useGameSessionStore"

export const eventHandler = (body : any) => {

    const store = useGameSessionStore();
    
}