CREATE TABLE round_icons (
                             id BIGSERIAL PRIMARY KEY,
                             color VARCHAR(255),
                             elemental_type VARCHAR(255),
                             image_url VARCHAR(255)
);